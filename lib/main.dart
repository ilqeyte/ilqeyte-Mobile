import 'app/main.dart' as app;

/// The build entry point. The bootstrap logic lives in [app.main] — this keeps
/// `lib/main.dart` at the package root so `flutter run` and `flutter build apk`
/// pick up the real app instead of a generated template.
void main() => app.main();
